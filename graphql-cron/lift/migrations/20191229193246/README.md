# Migration `20191229193246`

This migration has been generated by Inadati at 12/29/2019, 7:32:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."admins" (
  "email" text  NOT NULL DEFAULT '' ,
  "id" text  NOT NULL  ,
  "password" text  NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "admins.email" ON "public"."admins"("email")

CREATE UNIQUE INDEX "admins.password" ON "public"."admins"("password")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20191229192914..20191229193246
--- datamodel.dml
+++ datamodel.dml
@@ -1,28 +1,34 @@
 datasource db {
     provider = "postgresql"
-    url = "***"
+    url      = "postgresql://postgres:postgres@localhost:5432/postgres?schema=public"
 }
 model staffs {
-    id        String   @default(cuid()) @id
-    name      String
-    age       Int
+    id         String   @default(cuid()) @id
+    name       String
+    age        Int
     created_at DateTime @default(now())
     updated_at DateTime @updatedAt
 }
 model attends {
-    id        String   @default(cuid()) @id
-    staff_id String
-    in_time DateTime
-    out_time DateTime
-    is_attend Boolean
+    id              String   @default(cuid()) @id
+    staff_id        String
+    in_time         DateTime
+    out_time        DateTime
+    is_attend       Boolean
     date_start_time DateTime
-    created_at DateTime @default(now())
-    updated_at DateTime @updatedAt
+    created_at      DateTime @default(now())
+    updated_at      DateTime @updatedAt
 }
+model admins {
+    id       String @default(cuid()) @id
+    email    String @unique
+    password String @unique
+}
+
 // model User {
 //     id    String  @default(cuid()) @id @unique
 //     email String  @unique
 //     name  String?
@@ -36,5 +42,5 @@
 //     published Boolean
 //     title     String
 //     content   String?
 //     author    User?
-// }
+//
```


